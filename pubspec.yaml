name: spendwise
description: "A comprehensive personal finance and expense tracking application."
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.9
  shared_preferences: ^2.5.5
  intl: ^0.20.2
  uuid: ^4.5.3
  fl_chart: ^1.2.0
  csv: ^8.0.0
  provider: ^6.1.5+1
  path_provider: ^2.1.1
  file_picker: ^11.0.2
  url_launcher: ^6.2.4
  sqflite: ^2.4.2+1
  sqflite_common_ffi: ^2.4.0+3
  path: ^1.8.3
  flutter_local_notifications: ^21.0.0
  timezone: ^0.11.0
  image_picker: ^1.2.2
  math_expressions: ^3.1.0
  google_fonts: ^6.2.1
  web: ^1.1.0
  # camera: ^0.11.2 # Unused
  # http: ^1.5.0 # Unused
  # permission_handler: ^12.0.1 # Unused

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  mockito: ^5.6.4
  build_runner: ^2.15.0

flutter:

  uses-material-design: true